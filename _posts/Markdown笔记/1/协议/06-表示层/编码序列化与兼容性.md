# 编码、序列化与兼容性学习笔记

最后整理：2026-06-14

Last researched：2026-06-14

表示层关注“数据如何表示”。实际工程中，很多线上问题不是网络不通，而是编码、字段类型、时间格式、字节序、压缩、Content-Type、版本兼容出了问题。本篇把常见表示层问题集中整理。

## 学习目标

- 分清字符编码、数据格式、序列化协议、压缩、加密。
- 理解 UTF-8、JSON、MIME、Base64、字节序、时间格式的常见坑。
- 理解向前兼容、向后兼容、字段演进和版本协商。
- 能排查乱码、解析失败、字段丢失、时间错乱、二进制协议大小端错误。

## 表示层关心什么

| 类别 | 例子 | 解决的问题 |
|---|---|---|
| 字符编码 | ASCII、UTF-8、UTF-16、GBK | 字符如何变成字节 |
| 数据格式 | JSON、XML、CSV、YAML | 结构化数据如何文本表示 |
| 二进制序列化 | Protobuf、MessagePack、CBOR、ASN.1 DER | 更紧凑和类型化的数据表示 |
| MIME/媒体类型 | `application/json`、`text/html` | 内容是什么格式 |
| 压缩 | gzip、br、zstd | 减少传输体积 |
| 加密/签名 | TLS、JWS、CMS | 机密性、完整性、身份 |
| 字节序 | big-endian、little-endian | 多字节数值如何排列 |

## 字符编码

UTF-8 是 Web 和现代系统最常见编码。特点是兼容 ASCII、可变长、支持 Unicode、无需字节序，适合作为跨系统默认文本编码。

常见问题：

- 服务端实际返回 UTF-8，但 `Content-Type` 未声明或声明错误。
- 文件是 GBK，本地按 UTF-8 读取导致乱码。
- UTF-8 BOM 影响某些解析器。
- 字符串长度和字节长度混淆，导致截断错误。

```text
"abc"   字符数 3，UTF-8 字节数 3
"协议"  字符数 2，UTF-8 字节数 6
"😀"    用户看到 1 个符号，UTF-8 字节数 4
```

## JSON 常见坑

| 问题 | 说明 |
|---|---|
| 数字精度 | JavaScript Number 对大整数不安全 |
| `null` 与缺失 | 语义不同，兼容性要约定 |
| 时间格式 | ISO 8601、Unix 秒、Unix 毫秒容易混用 |
| 字段命名 | snake_case、camelCase、PascalCase 要统一 |
| 枚举扩展 | 客户端遇到未知枚举值应有兜底 |
| 布尔字符串 | `"false"` 是字符串，不是布尔 false |
| Content-Type | 应使用 `application/json` |

大整数建议用字符串传输；金额用整数最小单位或十进制定点字符串，不要用浮点数传精确金额。

## 时间表示

| 格式 | 示例 | 特点 |
|---|---|---|
| RFC 3339 | `2026-06-14T09:30:00+08:00` | 可读，带时区 |
| Unix 秒 | `1781391000` | 简洁，但单位需约定 |
| Unix 毫秒 | `1781391000000` | 前端常用 |
| 本地时间字符串 | `2026-06-14 09:30:00` | 易丢时区，不推荐跨系统 |

建议跨系统传输使用 UTC 或带时区的 RFC 3339 格式，并明确单位是秒、毫秒、微秒还是纳秒。

## 字节序

| 字节序 | 说明 | `0x1234` |
|---|---|---|
| Big-endian | 高位字节在前，网络字节序 | `12 34` |
| Little-endian | 低位字节在前 | `34 12` |

常见场景：

- TCP/IP 头部字段使用网络字节序，即 big-endian。
- Modbus 寄存器每个 16 位寄存器通常高字节在前，但 32 位浮点跨寄存器字序可能因厂家不同而不同。
- MCU、PC 本地内存字节序不一定等于协议字节序。

## Base64

Base64 用文本字符表示二进制数据，常用于 JSON、证书、邮件、Data URL。

注意：

- Base64 不是加密。
- 体积大约增加 1/3。
- URL 场景可能使用 Base64URL，字符集和 padding 不同。
- 换行、padding `=`、字符集差异会导致解析失败。

## 压缩与 Content-Type

HTTP 常见压缩由 `Content-Encoding` 表达：

```http
Accept-Encoding: gzip, br, zstd
Content-Encoding: gzip
```

`Content-Type` 告诉接收方如何解释内容：

```http
Content-Type: application/json; charset=utf-8
Content-Type: multipart/form-data; boundary=...
```

常见问题：

- 服务端压缩了但没设置 `Content-Encoding`。
- JSON API 返回 `text/plain`，导致客户端不按 JSON 解析。
- multipart boundary 错。
- 字符集声明错误导致乱码。

## 版本兼容

| 兼容性 | 含义 |
|---|---|
| 向后兼容 | 新程序能读旧数据 |
| 向前兼容 | 旧程序能忽略或处理新数据 |

API/协议演进建议：

- 新增字段通常安全，删除字段风险大。
- 不要改变已有字段含义。
- 枚举值新增要考虑旧客户端兜底。
- 字段类型不要随意变化，例如 string 改 number。
- 客户端忽略未知字段。
- 服务端容忍缺失的可选字段。

Protobuf 兼容建议：

- 不复用已删除字段编号。
- 删除字段后保留 reserved。
- 新增字段使用新编号。
- 不随意改变字段类型。

## 常见问题

| 现象 | 可能原因 | 排查方向 |
|---|---|---|
| 中文乱码 | 编码声明和实际字节不一致 | 查 `Content-Type`、文件编码、数据库连接编码 |
| JSON 解析失败 | 非法逗号、注释、NaN、单引号、压缩未解 | 保存原始响应体 |
| 金额精度错 | 浮点数传金额 | 改用整数分或 Decimal |
| ID 变了 | 大整数被 JavaScript Number 截断 | ID 用字符串 |
| 时间差 8 小时 | UTC/本地时区混用 | 统一 RFC 3339 或 UTC |
| Modbus 浮点异常 | 字节序/字序不同 | 对照寄存器文档和抓包 |
| 下载文件打不开 | MIME 错、压缩错、二进制按文本处理 | 看十六进制头和响应头 |
| 旧客户端崩溃 | 新增枚举或字段未兼容 | 客户端忽略未知值 |

## 排查工具

```bash
curl -I https://example.com
curl -v https://example.com
jq . file.json
file data.bin
xxd data.bin | head
base64 -d input.txt > output.bin
```

## 参考资料

- RFC 8259 - JSON: <https://www.rfc-editor.org/rfc/rfc8259>
- RFC 3629 - UTF-8: <https://www.rfc-editor.org/rfc/rfc3629>
- RFC 6838 - Media Type Specifications and Registration Procedures: <https://www.rfc-editor.org/rfc/rfc6838>
- RFC 4648 - Base-N Encodings: <https://www.rfc-editor.org/rfc/rfc4648>
- RFC 3339 - Date and Time on the Internet: <https://www.rfc-editor.org/rfc/rfc3339>
- Protocol Buffers Language Guide: <https://protobuf.dev/programming-guides/proto3/>
